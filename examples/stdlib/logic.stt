(fn) [a] not {
	true false
	(ifs) {a} {flip}
	drop
}
(fn) [a b] or {
	false true
	(ifs)
		{a} {flip}
		{b} {flip}
	drop
}

(fn) [a b] and {
	a true =
	a b =
		=
}

(fn) [a b] < { b a > }

(fn) [a b] >= {
	a b >
	a b =
		or
}

(fn) [a b] <= { a b > not }

